import 'package:flutter/material.dart';

class BookingRequestPage extends StatelessWidget {
  const BookingRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Booking Request')),
      body: const Center(child: Text('Booking Request')),
    );
  }
}
